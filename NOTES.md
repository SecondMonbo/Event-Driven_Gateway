# 事件驱动网关 - 项目笔记

- 纯个人记录与思考，会有很多碎碎念，可能不合规范，所以[项目结构介绍,请点击跳转 README](./README.md)
- 骨架直接ai跑了，边写边改吧

## 📌 项目概述

### 写这个的原因

- 主要还是为了记录自己的思考，一些功能的划分，层次梳理，代码结构的重构，再到debug，在当时能有比较清晰的思考与梳理过程，但过后可能只会剩下一些思考的碎片和没删除干净的调试输出，所以为了让我之后成为大手子，先从写一篇项目笔记开始吧😋

### 为什么写这个项目

- 项目名字是 "Event-Driven_system_for_Agent_Gateway", 事件驱动的智能体网关，听起来还不错，但实际就是常见的 WebServer + agent，这应该算是我学习网络编程的衍生品。当时决定就业时，开始了解一个应届毕业生该具备怎样的能力，根据现有的条件，我决择的方向是 C++后端，之后开始学习相关的技术栈。
- 在跟着黑马的网课学完linux操作系统与基本命令后，之后就是网络编程，想着我该试着写一个项目了
- WebServer是烂大街的，B站一搜资源很多，新颖的项目也多，为了让我之后可能遇到的面试官眼前一亮，对我产生兴趣，我似乎不该写这个，在当时我思考之后，觉得项目是普通的，但工程能力互通的，对一个只有语言基础与一些大学必修课的大学生来说，贸然去接触那些说的很新的项目，未必是一个好的选择
- 我的网络编程课是学校的选修课，包括理论课与实验课，zjy老师很厉害，包括技术与教学方面，zjy老师老师从数据包流转路径开始讲，所以跟着老师，自己逐步复现与扩展一个WebServer也不错
- 2026年上半年是Agent爆发后趋于成熟的一个时间段，牛客上刷的很多，方向python与JAVA的大手子都说是必需的。老师上课最后阶段演示的项目就是python的带智能体的网关，那我或许可以用C++复现一个Agent_Gateway，迎合市场需求，锻炼技术能力，也能接触到热门的AI相关概念，好处多多ψ
- 之后我开始抄抄写写改改，写到今天 2026/8/22 ，能停下来写写笔记，也是因为昨 天而实现了 集成DeepSeek的API，并能够调用工具获取当前时间，具备了一个Agent最基础的能力，就像一个孩子在通过爬行积蓄力量站起来迈出了第一步，虽有诸多不完善，但核心功能都已具备，之后就是让他逐渐壮大了

## 🏗️ 架构设计

- 整体分层（网络层、协议层、业务层）
- 核心模块职责（EpollLoop、ClientConnection、SseHandler、LLMService 等）

## 🧩 关键模块实现

### 网络层

- epoll 边缘触发 + 非阻塞 I/O
- 写缓冲区 + EPOLLOUT 机制

### 协议层

- HTTP/1.1 解析（状态机）
- SSE 流式输出

### 业务层

- SessionManager（会话管理）
- ToolRegistry + ToolExecutor（工具调用）
- DeepSeek API 集成

## 🐛 踩坑记录（重点！）

### 1. 值传递 vs 引用传递

- **问题**：ConnectionContext 按值传递导致数据不同步
- **排查**：日志发现 pending_llm 数据丢失
- **解决**：改为 const 引用传递

### 2. 工具名称解析失败

- **问题**：parse_tool_calls 提取 name 为空
- **排查**：打印 tc.dump() 发现结构正常但 name 未提取
- **解决**：补上 call.name = func["name"].get<std::string>()

### 3. DeepSeek API 401 认证失败

- **问题**：Bearer 和 API Key 之间缺少空格
- **解决**：改为 `"Bearer " + api_key`

## 📊 性能数据

- wrk 压测：QPS ~2.8 万
- Valgrind：definitely lost: 0

## 🚀 未来扩展方向

- 多轮工具调用
- WebSocket 支持
- RAG 集成
- Docker 容器化
